[project]
name = "cowswap-auctions"
version = "0.1.0"
description = "Fetch historical WBTC trades from CoW Protocol"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.1",
]
